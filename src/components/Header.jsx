import React from 'react';
import { LogOut, Languages } from 'lucide-react';
import { useAppStore } from '../store/useAppStore';
export default function Header(){const getSettings=useAppStore(s=>s.getSettings);const currentUser=useAppStore(s=>s.currentUser);const logout=useAppStore(s=>s.logout);const setLanguage=useAppStore(s=>s.setLanguage);const settings=getSettings();const langs=[['ar','عربي'],['en','EN'],['ur','اردو'],['bn','বাংলা']];return <header className="header"><div className="brand">{settings.logo?<img src={settings.logo} alt="logo"/>:<div style={{fontSize:28,fontWeight:900}}>N</div>}<div><b>{settings.systemName}</b><div style={{opacity:.85,fontSize:13}}>{settings.company} - Operations Intelligence Platform</div></div></div><div className="actions"><span>{currentUser?.name}</span><div className="langSwitch"><Languages size={16}/>{langs.map(([code,label])=><button key={code} className={`miniLang ${settings.language===code?'active':''}`} onClick={()=>setLanguage(code)}>{label}</button>)}</div><button className="btn light" onClick={logout}><LogOut size={18}/> خروج</button></div></header>}
